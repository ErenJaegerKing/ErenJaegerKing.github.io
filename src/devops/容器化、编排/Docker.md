---
title: "Docker"
description: ""
icon: ""
date: 2024-09-21
category:
  - 运维
tag:
  - Docker
---

## Docker
```
docker
	环境预安装
	镜像加速服务
		加速地址
		路由转发
	服务启动/关闭
	基本管理
		查看版本信息
		容器
			查看容器
			容器创建
			容器启动
			运行容器执行指令
				执行结束后，容器资源释放，安全退出
			容器停止
			进入容器
			容器导出
			容器导入
			容器删除
				先停止，再删除
			批量删除停止状态的容器
		镜像
			查找镜像
			查看本地镜像
			下载镜像
			获取镜像信息
			添加新标签
			删除镜像
				如果添加了新标签，删除掉的时候只能用名字删除，切勿用ID
				对于没有新标签的镜像，即可使用名字，也可用ID
			镜像备份
			镜像还原
		网络
			bridge
				创建容器默认连接的模式，分配IP，创建虚拟网卡，连接到docker0虚拟网桥，通过docker0网桥以及iptables nat表配置与宿主机通信
			host
				容器不会虚拟出自己的网卡，不会配置自己的IP，而是使用宿主机IP和端口
			none
				关闭网络功能
			自定义网络
				创建网络资源
				使用资源给容器指定地址
		dockerfile制作镜像
			镜像制作的工具
			镜像实战
				Apache镜像
					创建工作目录
					创建Dockerfile
					创建服务启动脚本run.sh
					默认首页
					构建镜像
					启动容器
					查看浏览器
				nginx镜像
					同上
				Tomcat镜像
					同上
		镜像私有仓库
			registry
				下载registry镜像
				配置daemon.json文件，添加仓库位置及端口
				重启docker服务
				使用docker run 开启仓库容器，提供服务
			上传镜像
				打标签
				上传
				查看仓库镜像
			镜像从私有仓库中下载
		数据卷/数据卷容器
			数据卷docker run  -v /var/www:/data1 --name web1 -it centos:7 /bin/bash
			数据卷容器
				存储容器
				使用存储容器空间进行挂载到其他容器中使用
		容器互联
			创建容器，保持UP状态
			使用link关联对方容器，建立互联通信
```